void main(){
  // int number;
  int? number2;

  // print(number);
  print(number2);
}

/**
 * null merupakan representasi dari data yang kosong
 * secara default, variable yang kita buat harus diisi, jika tidak maka tidak akan bisa digunakan
 * jika ingin membuat variabel yang kosong tanpa data maka kita harus menambahkan ? contoh: String? name; atau String? name = null;
 * null safety menggunakan "!"
 * untuk memastikan bahwa data tersebut tidak null
 */